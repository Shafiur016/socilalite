"""Traditional feature extraction for classical ML baselines."""

from typing import List


def build_tfidf(corpus: List[str]) -> None:
    """Placeholder for TF-IDF vectorization."""
    print(f"Building TF-IDF for {len(corpus)} documents")
