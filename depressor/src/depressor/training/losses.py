"""Loss utilities for multi-label and multi-class tasks."""

from typing import Any


def bce_loss(outputs: Any, targets: Any) -> float:
    """Placeholder binary cross-entropy loss."""
    return 0.0


def ce_loss(outputs: Any, targets: Any) -> float:
    """Placeholder cross-entropy loss."""
    return 0.0
