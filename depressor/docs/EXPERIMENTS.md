# Reproducing Experiments

Use the configuration files under `configs/` together with the scripts in `scripts/` to run preprocessing, training, calibration, and evaluation. Populate the `data/` directory with the appropriate splits before starting.
